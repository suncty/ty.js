require('ty')

(['express', 'async', 'fs'])

('hello.js')

('isArray', function(obj) {
    if(obj instanceof Array)
        return true;
    else
        return false;
})

('common', function() {
    console.log('test yeah!');
})

()